version: 1
runtime: python
entry: vault_health/server.py

build:
  install: pip install -e .

startCommand:
  type: http
  command: python -m vault_health --transport streamable-http

configSchema:
  source: code
